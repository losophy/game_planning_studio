[project]
name = "lead-agent"
version = "0.1.0"
description = "主策划Agent"
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.0.0",
    "langchain-openai>=0.3.0",
    "python-dotenv",
    "requests",
    "pyyaml",
]
