# game_planning_studio（AI游戏策划工作室）代码阅读指南

本工程是一个基于 LangChain + Flask 的双 Agent 游戏策划协作系统：纯 Python 约 700 行（两个 `main.py` + 7 个 SKILL.md 技能提示词），无前端，依赖由 uv 分目录管理。核心是「一主一从」的 A2A 协作架构——**主策划 Agent**（`lead_agent/`，CLI 客户端）从「老板一句话」产出主策划方案，通过**标准 A2A 协议**（Agent Card 发现 + JSON-RPC `tasks/send` 提交）把方案交给 **玩法策划 Agent**（`gameplay_agent/`，Flask 服务端），产出玩法策划方案。建议按「主策划流程 → A2A 协议 → Skills → 配置」的顺序读，先读发起方链路，再补协议服务端和提示词体系。

## 主干数据流

```
① 老板一句话
终端 input → lead_agent/main.py main() → 检查玩法策划是否在线

② 主策划执行链（lead_agent/main.py）
check_gameplay_agent → GET {card_uri}/.well-known/agent.json（A2A 发现，5s 超时）
  → lead_designer.invoke（LangChain Agent，tools=[]，SKILL.md 文本拼进 system_prompt）
  → 主策划方案 → 写入 output/主策划方案.md → summarize_md 本地摘要（零 LLM 调用）

③ A2A 递交（核心链路）
send_plan_to_gameplay → POST {gameplay_uri} JSON-RPC tasks/send（300s 超时）
  → gameplay_agent: handle_tasks_send → gameplay_designer.invoke → 玩法策划方案.md
  → 返回 Task{status:completed, artifacts:[text]}
  → 主策划从 artifacts 提取全文，同步写回 lead_agent/output/玩法策划方案.md
```

③ 中整条链路绕不开两个基础设施：`config.yaml`（两个 Agent 唯一的「耦合点」，跨电脑协作只改它）和 `TASK_STORE`（玩法策划的内存任务表，重启即清）。与 gamesearch 的 ContextVar/WebSocket 实时回传不同，这里是最朴素的「同步 HTTP 请求-响应」——主策划 POST 后要等玩法策划跑完一次完整 LLM 调用才返回，所以超时设了 300s。

## 推荐阅读顺序

### 第 0 步：先建立整体认识

- 读 `README.md` 的「目录结构」和「快速开始」—— 两条主线分别是「双 Agent 分工」和「同机/跨电脑两种运行方式」。
- 读 `AI游戏策划工作室搭建方案.md`（v6.1）—— 完整设计文档：第 1 章工作流程、第 2 章架构图（第 59-85 行画了跨电脑 A2A 拓扑）、第 5.3 节 A2A 接口规范、第 6 章 Skills 设计、第 7 章使用方式、第 9 章跨电脑网络配置。
- 读 `参考原型/` —— 腾讯魔方工作室 Morre 的《用 CodeBuddy 多 Agent 协作做一款肉鸽卡牌游戏》实战文章 + 16 张截图，是本项目的设计灵感来源。
- 读两个 `config.yaml` —— 各 16 行，把「谁能找到谁」一次看清。

### 第 1 步：`lead_agent/main.py`（用户入口，主流程）

- `main.py:24` `load_config` —— 读 `config.yaml`，**文件缺失时返回内置默认 URI**，开发期容错设计。
- `main.py:48` `get_llm` —— 从本 Agent 的 `.env` 读 `LLM_MODEL` / `LLM_API_KEY` / `LLM_BASE_URL`（兼容 `DEEPSEEK_API_KEY`）。每个 Agent 独立 `.env`，**两个策划可以用不同 LLM**。
- `main.py:70-108` `LEAD_DESIGNER_PROMPT` —— 本质是「技能注入模板」：把 `need_analysis` 和 `project_planning` 两个 SKILL.md 文本直接拼进 system_prompt。**本项目没有独立的技能加载框架，Skill = 提示词文本**，想调主策划行为改这里或改 SKILL.md 即可。
- `main.py:111-116` `create_agent(tools=[])` —— 注意 **tools 为空**：纯 LLM 无工具调用，和 gamesearch 的「工具驱动」完全不同，两 Agent 之间不靠工具、靠 HTTP 协议协作。
- `main.py:119-132` `check_gameplay_agent` —— A2A 发现：GET `/.well-known/agent.json` 拿能力名片；失败不阻塞主流程（`main.py:257` 玩法策划不在线时先产出主策划方案存档，等其到位后重跑补齐）。
- `main.py:135-172` `summarize_md` —— 纯本地 Markdown 解析（文档标题 + 游戏概述首句 + 章节列表），零 LLM 调用。**省 token 的巧思**，不用为「看一眼摘要」花一次模型调用。
- `main.py:174-229` `send_plan_to_gameplay` —— 全链路最核心：拼 JSON-RPC 2.0 payload（`main.py:177-190`）→ `requests.post(GAMEPLAY_URI, timeout=300)`（`main.py:192`）→ 解析返回 Task → 从 `artifacts` 里提取玩法方案全文写回本地（`main.py:207-215`）。
- `main.py:273-278` —— `invoke` 后取 `messages[-1].content`：最后一条才是 Agent 回答，第一条是用户输入，别取反。

### 第 2 步：`gameplay_agent/main.py`（协议服务端，A2A 核心）

- `main.py:160` 创建 Flask 应用；`main.py:163-167` 静音开发服务器日志的骚操作：`werkzeug`/`flask.cli` 降到 ERROR + 直接替换 `show_server_banner`，让控制台只剩业务输出。
- `main.py:171` `TASK_STORE` —— 内存字典存 Task（task_id → Task），**重启即清、无持久化**，够演示用。
- `main.py:176-199` `agent_card` —— A2A Agent Card「能力名片」：name / description / capabilities / skills / defaultSkillId，供调用方发现与握手。
- `main.py:215-276` `handle_tasks_send` —— 任务状态机：先登记 `working`（`main.py:234`）→ `gameplay_designer.invoke`（`main.py:238`）→ 写 `output/玩法策划方案.md`（`main.py:247`）→ 构造 `completed` Task 并把全文塞进 `artifacts`（`main.py:263-268`）；异常则置 `failed`。
- `main.py:289-293` `JSONRPC_METHODS` —— 方法分发表：`initialize` / `tasks/send` / `tasks/get`，加方法只需往这张表里注册。
- `main.py:300-336` 根路径 JSON-RPC 端点 —— 错误码 `-32601`（方法不存在）/ `-32602`（参数错）/ `-32603`（内部错误）是 **JSON-RPC 2.0 标准码**，不是自定义的。
- `main.py:338-345` `/health` 健康检查；`main.py:347-349` `run_flask` 监听 `0.0.0.0:8080` 且 `threaded=True`，支持并发请求。

### 第 3 步：Skills 体系（`shared/` + `skills/`，改行为的主战场）

- 加载机制极其简单：`load_skill`（lead `main.py:57` / gameplay `main.py:51`）只是读文件返回字符串，然后拼进各自的 `*_PROMPT`。
- 7 个 SKILL.md 的分工：
  - `need_analysis/`（需求分析）—— 共享技能，两个 Agent 各带一份，**内容完全一致**（`shared/skills/need_analysis/` 是源，改动后需手动同步到两处）。
  - `project_planning/`（项目规划）—— 主策划专属。
  - `core_loop/`（核心循环）、`combat_system/`（战斗系统）、`numerical_balance/`（数值平衡）—— 玩法策划专属。
- 每个 SKILL.md 都是同一结构：frontmatter（name/description）+ 角色定位 / 触发边界 / 执行步骤 / 输出格式 —— 本质是给 LLM 看的「岗位说明书」。

### 第 4 步：配置与依赖（想换模型 / 跨电脑看这里）

- `config.yaml` 是双 Agent 唯一的耦合点：lead 的 `gameplay_uri` / `gameplay_card_uri` ↔ gameplay 的 `uri` / `port`。跨电脑协作时：电脑 B 把 gameplay 的 `uri` 改成自己 IP，电脑 A 把 lead 的两个 URI 改成电脑 B 的 IP，再加防火墙放行 8080（方案第 9 章）。
- `pyproject.toml` 依赖差异只有两个：lead 多 `requests`（HTTP 客户端）、gameplay 多 `flask`（HTTP 服务端）；`langchain>=1.0.0` 才有 `create_agent` 这个 API（旧版是 `initialize_agent`）。
- uv 管理：`uv sync` 自动建 `.venv` + 生成 `uv.lock`，两个 Agent 各自独立环境。

### 第 5 步：设计文档与参考原型（按需翻阅）

- `AI游戏策划工作室搭建方案.md` 第 9 章 —— 跨电脑的防火墙（Windows/Linux/macOS 三套命令）与路由器端口转发配置。
- `参考原型/` —— 真实项目的多 Agent 协作方法论：Agent 分工、文档结构与数据资产、宪法层（让 AI 遵守项目规则）等，对理解「为什么这么拆 Agent」很有帮助。

## 阅读姿势建议

1. **对照 git 历史读**：`git log --oneline` 共 11 个提交，演进脉络清晰：first commit → 重构项目结构 → 迁移到 uv → PyInstaller 打包（后移除，改为直接 `uv run`）→ Markdown 摘要功能 → 简化接收流程 → 添加参考原型 → **升级为标准 A2A 协议**（最新）。能看到项目「从演示脚本到标准化协议」的演进。
2. **边读边画调用图**：

   ```
   lead_agent/main.py → requests → POST gameplay_agent A2A 端点（tasks/send）
   lead_agent/main.py → skills/{need_analysis, project_planning}/SKILL.md（拼进 prompt）
   gameplay_agent/main.py → skills/{need_analysis, core_loop, combat_system, numerical_balance}/SKILL.md
   两个 main.py → config.yaml（URI 耦合）+ .env（模型配置）+ pyproject.toml（uv）
   shared/skills/need_analysis → 手动同步 → lead 与 gameplay 各一份
   ```

3. **找扩展点**：
   - 想加第三个 Agent（如数值策划、美术策划）：仿照 gameplay 写一个 A2A 服务端 → 在 lead 的 `config.yaml` 加 URI → 仿 `send_plan_to_gameplay` 写一个发送函数。注意目前 lead 只处理了 `completed` 状态，流式/推送都不支持（Agent Card 里 `streaming: False`）。
   - 想加一个策划技能：在 `shared/skills/` 新建目录写 SKILL.md → 同步到目标 Agent 的 `skills/` → 在对应 `main.py` 里 `load_skill` + 拼进 prompt（三处改动）。
   - 想调 Agent 行为：改 SKILL.md / `*_PROMPT`，**不用动代码**；想换模型：改对应 Agent 的 `.env`；想跨电脑：改 `config.yaml`。
4. **协议验证不依赖任何 Agent 逻辑**：`gameplay_agent` 起起来后可以直接 curl 打 A2A 接口，看协议本身是否通：

   ```bash
   curl http://localhost:8080/.well-known/agent.json          # 发现：Agent Card
   curl -X POST http://localhost:8080/ -H "Content-Type: application/json" \
     -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}'   # 握手
   curl http://localhost:8080/health                          # 健康检查
   ```

**最快路径**：终端 1 先 `cd gameplay_agent && uv run python main.py` 起玩法策划，终端 2 `cd lead_agent && uv run python main.py` 起主策划，输入「我想做一个二次元风格的卡牌RPG手游」，观察完整链路：主策划方案落盘 → 摘要展示 → 递交 → 玩法策划方案落盘并同步带回；然后再 curl 一遍上面的三个接口，把 A2A 协议本身读透，最后回头补 Skills 与配置细节。
